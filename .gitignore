# Rust
/target/
**/target/

# Node
node_modules/
dist/
.vite/

# Tauri
app/src-tauri/target/
app/src-tauri/gen/

# 本地工件区（不入 git）
.plans/

# 构建产物
/release/
*.exe
*.msi
*.zip

# Python 字节码缓存。package-extras.py / collect-bundle.py 用 importlib 加载
# build-release.py，会在 scripts/ 下生成 __pycache__——纯本地产物，不入库。
__pycache__/
*.pyc

# 编辑器与系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 本地环境
.env
.env.local

# openspec/ —— 工件归 Gitea 私仓 Gitruck/steadcopy-openspec（2026-08-11 主理人拍板分流）
#
# ⚠️ 永不移除本条：这些工件含与前身项目的 clean-room 继承边界（逐条点名了它的
# 具体缺陷）与决策台账里的内部判断，主仓将来要公开，不该跟着走。
#
# openspec/ 现在是本仓的 submodule（见 .gitmodules）。本条对那个 gitlink 条目**无效**
# （它是 `submodule add -f` 加的），保留本条是兜底：万一哪天 submodule 被 deinit、
# openspec/ 变回普通目录时仍有屏蔽。
# 真正的硬闸是 .git/hooks/pre-commit（装法见 openspec/hooks/install.sh）。
openspec/

# 更新包签名私钥 —— **绝不进仓**。
# 拿到它的人可以签出一个「合法」的更新包，让所有客户端静默安装任意程序。
# 公钥编在 tauri.conf.json 里（那是公开的、也必须公开）；私钥只存两处：
# 发布机本地，与 GitHub Actions 的 repository secret（TAURI_SIGNING_PRIVATE_KEY）。
.updater-key
.updater-key.pub
*.key

# 发布操作手册 —— 自己看的，不入库。
# 里头有内网路径（NAS 映射盘）、私钥存放位置、GitHub 账号上的操作步骤，
# 都是主仓将来公开时不该跟着走的东西。
# 用户看的下载与校验说明在 README.md 与 docs/verify-download.md；
# 发布门控清单 docs/release-checklist.md 是入库的（它是「不发」的依据，
# 属于工程约束，不含内网信息）。
发布手册.md
